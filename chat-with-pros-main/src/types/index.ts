// Re-export all shared domain types from a single import path.
// Existing code importing from "@/lib/types" continues to work unchanged.
export type { Professional, Message, MessageStatus } from "@/lib/types";
